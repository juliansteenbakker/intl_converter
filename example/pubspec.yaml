name: example
description: intl_converter example.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.3

  intl: ^0.17.0
  intl_generator: ^0.2.0+0
dependency_overrides:
  meta: ^1.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  intl_converter:
    path: ../ #in real project you need depend on dart pub repository(intl_converter: ^0.0.2)

flutter:
  uses-material-design: true